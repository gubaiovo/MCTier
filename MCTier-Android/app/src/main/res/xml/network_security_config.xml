<?xml version="1.0" encoding="utf-8"?>
<!--
    私人信令节点由用户在运行时输入，可能是任意域名、局域网 IP 或公网 IP，
    因而无法使用编译期 domain-config 白名单。只在传输策略层允许明文；输入仍由
    LobbyInviteCodec 限制为 ws/wss 且禁止凭据、控制字符和可执行协议。
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
